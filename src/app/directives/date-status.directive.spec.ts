import { DateStatusDirective } from './date-status.directive';

describe('DateStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new DateStatusDirective();
    expect(directive).toBeTruthy();
  });
});
